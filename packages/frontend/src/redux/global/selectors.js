// Selektor do odczytania stanu isModalLogoutOpen
export const selectIsModalLogoutOpen = (state) => state.global.isModalLogoutOpen;

// Selektor do odczytania stanu isModalAddTransactionOpen
export const selectIsModalAddTransactionOpen = (state) => state.global.isModalAddTransactionOpen;

// Selektor do odczytania stanu ładowania (isLoading)
export const selectIsLoading = (state) => state.global.isLoading;

